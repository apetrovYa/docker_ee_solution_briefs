..  _grafana_prometheus_monitoring_prometheus_yml:

..  raw:: latex

    \newpage

prometheus.yml file
===================

This is a sample **prometheus.yml** configuration file which configures Prometheus. It must exist in the same directory as the :ref:`docker-compose.yml<grafana_prometheus_monitoring_docker_compose_yml>` file. 

Click :download:`here <samples/prometheus.yml>` to download the sample **prometheus.yml** file.

..  literalinclude:: samples/prometheus.yml
    :language: yaml  

..  toctree::
    :hidden:
    :titlesonly:
    :maxdepth: 1  
           